# Builder: usage-dashboard-page

User-facing usage dashboard showing:
- Current usage vs quota limits for all metrics
- Progress bars with color coding (green/yellow/red)
- "Exceeded" and "X left" badges
- Upgrade CTA for free users

**Path**: Accessible at `/<USAGE_PATH>` (default: `/usage`)

**Requires**: usage-metering-schema builder applied first.
