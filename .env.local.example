# Test Environment Variables
# Add these to .env.local to resolve build issues

# OpenRouter API (for DeepSeek access) - Use your actual key from Vercel
OPENROUTER_API=test_key_from_vercel_env

# Disable build-time API calls during development
NODE_ENV=development

# Optional: Database (SQLite for local development)
SQLITE_PATH=./data/geo-seo.db

# Optional: Other services (add as needed)
# FIRECRAWL_API_KEY=optional
# GOOGLE_API_KEY=optional