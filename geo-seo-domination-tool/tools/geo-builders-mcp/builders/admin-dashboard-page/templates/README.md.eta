# Builder: admin-dashboard-page

Creates a protected admin dashboard page showing all users with role management functionality.

**Features**:
- Admin-only access (checks role === 'admin')
- User table with email, name, role, created date
- Inline role update forms
- User statistics (total, pro, admin counts)
- Responsive layout with Tailwind CSS

**Requires**:
- `supabase-admin-role` builder (RLS policies for admin access)
- `/api/admin/update-role` endpoint (from `admin-users-api` builder)

**Path**: Accessible at `/<ADMIN_PATH>` (default: `/admin`)
